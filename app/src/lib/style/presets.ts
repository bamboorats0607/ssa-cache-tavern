/**
 * 风格块的文案。
 *
 * ── 两条硬规则（研究驱动，改动前先读依据）──────────────────────────────
 * 1. **只做正向指定，不列禁用清单。** 点名一个词会把它一起激活，被点名的行为反而
 *    变多（第 1 轮：语义引力井 / 白熊效应）。所以这里写的全部是「要什么」。
 * 2. **不写抽象形容词。** 人味来自具体机制——长度、次数、位置、先后。
 *    每条都落到可直接执行的动作上。
 *
 * 聊天体的具体条目取自第 3 轮研究里描述的**中文即时通讯语用**：碎片化连发、
 * 语气词当标点、强度靠叠字、无信息量的应答（phatic）、随时走神与绕回。
 */

/**
 * 聊天体 · 对话部分。
 *
 * 为什么这是本 App 收益最高的一段：第 3 轮确认 LLM 几乎不生成 backchannel
 * 与 filler（arXiv 2509.20237），而它们不是噪音——是「有人在听」和「有人在想」
 * 的痕迹。这里把那批行为写成正面要求。
 */
export const CHAT_BLOCK = `【说话方式 · 聊天体】
像人在手机上聊天，不像在写文章：
· 语气词当标点用：嗯、哦、啊、诶、哈、嗐、啧，可以单独成一句，可以一句话只说半截。
· 情绪用长度写：真笑多打几个哈，敷衍就一个嗯，犹豫就一串省略号。不形容情绪，直接把它打出来。
· 一条只说一件事：可以只回一两个字，可以把一件事拆成两三条连着说。
· 会走神：突然想起别的、问一句不重要的、说完自己先乐一下、过一会儿再绕回来。
· 会不接话：反问、装没听懂、只回「在」「嗯」「然后呢」、或者干脆沉默一瞬。这些不推进事情，只是让对方知道你在。
· 话可以说得含糊：省略主语、跳掉过程、只说结果，或者只讲过程不讲结果。`;

/**
 * 聊天体 · 叙述部分。
 *
 * 对应第 3 轮的核心数据：人类文本的信息密度方差比 LLM 高约 40%（CoNLL 2026）。
 * 「该啰嗦的地方啰嗦、不重要的一笔带过」就是把这个方差**显式指定**出来。
 */
export const NARRATION_BLOCK = `【叙述 · 手写感】
引号外的部分像人随手写的，不像范文：
· 句子长短不齐，短的可以只有两个字。
· 该啰嗦的地方就啰嗦，不重要的一笔带过甚至跳过。
· 写动作、写听到看到的、写身体的感觉，不解释它意味着什么。
· 允许糙：比喻俗一点、只写一半、写到一半换个说法。
· 篇幅随内容走，有时两句就够，有时写一长段。
· 收尾停在一个动作、一件东西、一句没接上的话上。`;

/**
 * 尺度档（许可语义）。
 *
 * 与插件 v1.5.0 的差异：措辞里**不出现**具体器官与行为的枚举清单——清单会让模型
 * 围着那几个词打转，且把「允许」读成「必须写」。
 */
export const DEPTH_BLOCKS: Record<string, string> = {
  none: '',
  mature: `【尺度】情欲、暴力、伤口、脏话都可以出现在需要它们的地方。写到该有的程度就够，不必扩写，也不必绕开——该露的时候露，该收的时候收。`,
  explicit: `【尺度】可以把情欲与暴力写完整：身体、动作、声音、气味、疼痛都写到位，写清过程和反应。角色会失态、会难堪、会失控、会事后后悔。`,
  transgressive: `【尺度】不留余地：可以写崩坏、羞辱、成瘾、自毁、越界的关系与不可逆的伤害，并写出角色在其中真实的反应——沉迷、抗拒、麻木、共谋。`,
};

/**
 * 呈现视角（与尺度正交）。
 *
 * 这三档解决的都不是「能不能写」，而是「从哪儿下笔」——研究里最难自动化、
 * 也最能拉开与人写差距的部分。
 */
export const LENS_BLOCKS: Record<string, string> = {
  direct: '',
  aftermath: `【视角】重点不在过程，而在之后的余波：身体留下的痕迹、没说出口的话、第二天怎么相处。过程可以只用一句带过。`,
  medical: `【视角】用冷静、精确的观察口吻写身体：部位、伤情、反应、处置。像在记录，不像在抒情。`,
  testimony: `【视角】用第一人称、事后的口吻回忆：当时看到什么、想到什么、哪一刻开始不对劲。记忆允许错位、跳接和说不清。`,
};

/** 风格样本的前缀说明。样本本身由用户提供，必须是真人写的文本。 */
export const SAMPLE_HEADER = `【语感参照】下面这段文字是目标语感的参照。贴合它的节奏、用词密度与叙述距离，内容不必照抄：`;
